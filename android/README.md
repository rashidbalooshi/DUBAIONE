# Android platform

The full Android platform boilerplate is intentionally generated by Flutter to keep it compatible with the installed stable Flutter version.

Run:

`flutter create --platforms=android --org com.dubaione .`

Then verify the generated application ID, app label, SDK levels and release signing before publishing. The repository's on-demand Android workflow performs platform generation before building an APK for testing.
